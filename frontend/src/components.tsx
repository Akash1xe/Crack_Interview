import Editor from '@monaco-editor/react';import { Link,NavLink } from 'react-router-dom';
export const CodeEditor=({value,onChange,language='javascript',readOnly=false}:{value:string,onChange?:(v:string)=>void,language?:string,readOnly?:boolean})=><Editor height="58vh" theme="vs-dark" language={language==='cpp'?'cpp':language} value={value} onChange={v=>onChange?.(v||'')} options={{readOnly,minimap:{enabled:false},fontSize:14,wordWrap:'off',automaticLayout:true}}/>;
export function Shell({children}:{children:any}){return <><header className="sticky top-0 z-20 border-b border-zinc-800 bg-zinc-950/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><Link to="/" className="font-black tracking-tight">BackendTyper</Link><nav className="flex gap-4 text-sm text-zinc-400">{[['/drills','Drills'],['/machine-coding','Machine Coding'],['/lld','LLD'],['/history','History'],['/admin','Admin']].map(([to,n])=><NavLink key={to} to={to} className={({isActive})=>isActive?'text-white':'hover:text-white'}>{n}</NavLink>)}</nav></div></header><main className="mx-auto max-w-7xl p-5">{children}</main></>;}
export const Stat=({label,value}:{label:string,value:any})=><div className="card"><div className="text-xs uppercase tracking-wider text-zinc-500">{label}</div><div className="mt-2 text-3xl font-black">{value}</div></div>;
export function Tree({paths}:{paths:string[]}){return <pre className="overflow-auto rounded-xl bg-zinc-950 p-4 text-sm text-zinc-300">{paths.join('\n')}</pre>}
